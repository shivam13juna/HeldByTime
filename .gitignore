# Build artifacts (generated by ./run_tests)
/build/

# macOS
**/*.DS_Store

# Go helper build cache (vendored deps are committed; binaries are not)
/helper/vaultseal
/helper/constdump

# Hand-assembled app bundle (produced only in Task 11)
*.app
